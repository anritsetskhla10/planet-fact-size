import { TPlanet } from "../../types/Types";

interface StructureSectionProps {
    planetObject?: TPlanet;
  }

export default function StructureSection({planetObject} : StructureSectionProps) {
  return (
    <div>
      
    </div>
  )
}
