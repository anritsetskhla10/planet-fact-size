import { TPlanet } from "../../types/Types";

interface OverviewSectionProps {
  planetObject?: TPlanet;
}

export default function OverviewSection({ planetObject }: OverviewSectionProps) {
  return (
  <>
  
  </>
  );
}